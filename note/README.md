# QuickShop-Hikari 项目要点笔记

> 本笔记是对整个项目源码阅读理解的要点提炼（2026-08-16 整理，关键结论已对照源码逐一核实）。
> 完整深度分析报告见 `codebase-analyzer/reports/QuickShop-Hikari-2026-08-16/`（含函数级调用链与变量级数据流）。

## 项目快照

| 项 | 值 |
|---|---|
| 项目 | quickshop-hikari `6.3.0.0-SNAPSHOT-6`（根 pom.xml） |
| 类型 | Minecraft Paper 服务端商店插件（GPL/AGPL v3 双许可） |
| 语言/JDK | Java 21 |
| 构建 | Maven 多模块 + Shade 重定位（fat-jar 含全部 addon/compat） |
| 目标平台 | 仅 Paper（Spigot 直接拒绝启动）；Folia 兼容 |
| 代码规模 | quickshop-bukkit 217 用例全绿的裁剪后规模：api/bukkit/common/platform 主体 + addon 7 个启用 + compat 10 个启用（R37 裁剪后，见 [06-功能裁剪-R37.md](06-功能裁剪-R37.md)；betonquest/angelchest 维持停用） |
| 数据库 | MySQL 或 H2(MODE=MYSQL)，schema 版本 20，EasySQL + HikariCP |
| 测试 | quickshop-bukkit 221 用例全绿（R38–R46 新增 44 用例：分配轴改写等价、哈希契约/世界快照回归、定位链路由、菜单物品访问与保存编码 memo 等价、方块状态快照门控契约、经济 provider 参透、文本渲染管线编译化与 fillArgs 预扫、权限节点记忆与设置事件构造门控；浸泡用例因本机 IO 劣化暂超时，见 note/06；回归 + 查找表索引 + DB 写缓存 + 文本缓存 + QUser 驻留 + 事件快路径 + 匹配免克隆 + 迭代器快照 + 木牌调度 + 点击路径 + 交易观测 + 指标批处理 + 剩余 DB 写批处理 + 文本预解析等价 + 日志懒队列 + 展示物重送 + 菜单库存快照 + 开箱扫描快路径 + 签名渲染单扫 + 区块加载快门 + 签名排程去重 + 杂项热路径快照 + 价格格式化快照 + 监听/展示物配置快照 + 库存操作变更日志 + 扫描预闸快速计数 + 方块状态快照门控 + 经济 provider 参透） |
| 基准 | `benchmark/` 独立模块：53 用例 × 9 套件（R31 起，热点 mock 全 stubOnly）（查找表/序列化/经济/文本/H2 数据库（含指标与外部缓存插入）/交易热路径（含 action 全路径与定位链）/监听器点击、展示物与搬运门控/菜单库存流水与分组排序），报告见 [report/perf](report/perf/)；快照化轮次起，涉配置用例须在 `Env.install()`→`Util.initialize()` 之前布置配置（display-type 已前置注入），晚置配置会测到「快照未初始化」哨兵路径并跨套件污染分配状态；R38 起 `-Dbenchmark.alloc` 提供近乎确定性的 B/op 分配轴（时序轴残余 ~50% 为 Mockito 栈噪声时的稳定验收轴） |
| 本仓库定位 | 独立 fork（已移除 upstream，不再同步社区上游） |

## 一句话理解这个项目

玩家在容器方块上放告示牌即可创建商店（买/卖店），点击告示牌触发交易；
核心难点是「经济转账 + 库存过户」两段式一致性与 Folia 多线程环境下的方块操作调度。

## 核心发现（最重要 5 条）

1. **「胖壳+大脑」双阶段引导**：`QuickShopBukkit`（libby 运行时装库 → Paper 平台检测）→ `QuickShop`（40+ 管理器装配）。经济系统刻意延迟 1 tick 装载以等待 Vault 注册（`QuickShop.java:869`），另有 `EconomySetupListener` 监听 PluginEnableEvent 双保险。
2. **一致性靠补偿而非锁**：经济与库存双栈均采用「快照 + Operation 命令模式 + LIFO 回滚」，业务统一入口 `safeCommit()` / `failSafeCommit()`；SQL 全部跑在 Java 21 虚拟线程（`QuickExecutor.java` newThreadPerTaskExecutor）。
3. **三表分离持久化 + 数据行去重**：`qs_shop_map`（坐标→店ID）⋈ `qs_shops`（店→data）⋈ `qs_data`（实质数据）；`updateShop` 先按字段查重复用 data 行，多个商店可共享同一行（`SimpleDatabaseHelperV2.java`）。
4. **⚠ 曾发现多处资金缺陷，2026-08-16 审计循环已全部修复**（详见 [04-开发速查.md](04-开发速查.md) 修复记录表）：
   - `checkTax` 守卫写反 → 正税额从不入账（已修 472e773bb）；
   - 经济/库存 commit 两个失败分支漏调 `onFailed`（已修 e19f7407d/8d71e34ef）；
   - **交易数量溢出/零单位刷钱漏洞**：unitSize=0 或溢出为 0 时移 0 件物品却全额转账（已修 9c88938d5）。
5. **测试与性能基线演进**：审计循环建立了回归网（38 用例）；2026-08-16 第一轮性能优化循环（7 轮，详见 [report/perf/2026-08-16-performance-optimization.md](report/perf/2026-08-16-performance-optimization.md)）后达 56 用例全绿，并新增 `benchmark/` 基准模块（19 用例 × 5 套件）。核心收益：id/owner 查找 O(n)→O(1)（142μs/282μs→~100ns）、脏店保存不变跳写 -81%、无参消息渲染 -99%、全量读店 -38%、指标定位 SELECT -99.6%。
6. **第二轮性能优化循环（交易与交互热路径，R8–R12 + R14，详见 [report/perf/2026-08-16-trade-path-optimization.md](report/perf/2026-08-16-trade-path-optimization.md))**：买卖全链路 -62.7%/-61.2%、库存扫描 -44%/-38%、迭代器 O(n²)→O(n)（纯迭代 -98.1%）、交易后木牌更新批处理合并、点击路径方块访问削减（R13 带参文本参数序列化尝试实测无收益已回退并记录）。测试增至 70 用例全绿、基准扩至 28 用例 × 7 套件。
16. **第十二轮（R24·开箱守卫扫描快路径，2026-08-26，详见 [report/perf/2026-08-26-inventory-check-fastpath.md](report/perf/2026-08-26-inventory-check-fastpath.md)）**：InventoryOpenEvent 全服触发的守卫物 54 槽扫描在虚拟展示模式（默认）下可证恒假，一次模式判定整段跳过（基准 **-99.6%**，2.09ms→8.1μs，六 fork 零重叠；真实成本模型下 15~30μs→0.3μs/次开箱）。测试 118 用例全绿、基准 39 用例。

17. **第十三轮（R25·签名渲染管线，2026-08-27，详见 [report/perf/2026-08-27-sign-render-pipeline.md](report/perf/2026-08-27-sign-render-pipeline.md)）**：木牌刷新（每笔交易后/漏斗供料后经 SignUpdateWatcher 落主线程）的 render 对同一库存量双扫（header 的 inventoryAvailable 与 trading 行的 remainingStock 在内置类型下同源）压为单扫共享（第三方店型保持独立双查保精确）、layout 模板按店型缓存（reload 失效）、renderItem 标志快照、setSignText 每牌子 config 读取提出循环（基准 **-47.6%**，1.86ms→0.98ms，六 fork 零重叠）。测试 126 用例全绿、基准 40 用例。

18. **第十四轮（R26·区块加载路径双快门，2026-08-27，详见 [report/perf/2026-08-27-chunk-load-fastgates.md](report/perf/2026-08-27-chunk-load-fastgates.md)）**：全服最高频事件之一的区块加载处理——守卫物孤儿清扫在虚拟展示后端（默认）下可证恒假整段跳过（getEntities 快照+逐实体检查→1 次后端判定，AbstractDisplayItem.canProduceGuardItems 与 checkIsGuardItemStack 早退同源）；无商店区块 isEmpty 早退（原 null 检查在 R2 返回 emptyMap 后永不触发，每次加载白付 chunkName 拼接+PerfMonitor+无条件 performance 空记录）；非虚拟模式孤儿清扫行为不变（基准 **-95.8%**，560μs→23.8μs，六 fork 零重叠）。测试 133 用例全绿、基准 41 用例。

19. **第十五轮（R27·签名排程去重 O(1)，2026-08-27，详见 [report/perf/2026-08-27-sign-schedule-dedup.md](report/perf/2026-08-27-sign-schedule-dedup.md)）**：漏斗/交易触发的 scheduleSignUpdate 去重从待处理队列线性扫描（500ms 窗口内全部商店数，高频漏斗服务器 O(n²)/窗口）改为伴生 ConcurrentHashMap.newKeySet（ContainerShop 身份等价使两者语义严格一致；排空移除、可重排、首胜 locale 保持）（基准 **-98.6%**，2.25μs→32ns，六 fork 零重叠，绝对值即真实量级）。测试 135 用例全绿、基准 42 用例。

20. **第十六轮（R28·杂项热路径复合，2026-08-27，详见 [report/perf/2026-08-27-misc-hotpath-composite.md](report/perf/2026-08-27-misc-hotpath-composite.md)）**：三处求值策略替换——Util 物品名双 config 标志 volatile 快照（initialize() reload 钩子刷新，构件级基准 **-99.5%** 零重叠）、ChatListener 聊天门控快照（被取消聊天链中位数 **-29.3%**）、漏斗/投掷器监听 InventoryHolder 双次获取合并；PlayerEvent.getPlayer 为 final 的反射注入法与「被优化构件直接计量」范式留档。测试 140 用例全绿、基准 44 用例。

21. **第十七轮（R29·价格显示链，2026-08-27，详见 [report/perf/2026-08-27-price-chain.md](report/perf/2026-08-27-price-chain.md)）**：EconomyFormatter/BuiltInEconomyFormatter 的 alternate-currency-symbol 快照（内部格式化兜底路径每次 getString → reload 刷新字段；签名/收据/菜单价格与 Vault 空返回兜底均经此路，构件基准 **-98.4%** 六 fork 零重叠）+ MsgUtil.decimalFormat 共享 DecimalFormat 改 ThreadLocal（**红线内稳定性修复**：Folia 区域线程并发渲染价格时共享非线程安全 NumberFormat 有错乱风险）。测试 144 用例全绿、基准 45 用例。至此库内已知热点均有结论。

22. **第十八轮（R30·交易链配置访问清扫，2026-08-27，详见 [report/perf/2026-08-27-trade-chain-config-sweep.md](report/perf/2026-08-27-trade-chain-config-sweep.md)）**：系统性 grep 甄别法替代定向发现——actionBuying/actionSelling 每笔交易的 pay-unlimited-shop-owners 改用既有快照字段、店主通知 show-tax 新增独立快照字段（与 shop-tax.show 不同源易错点）、ShopUtil「all」计算路径静态快照挂 Util.initialize 钩子；每笔交易省 2-3 次 config 树行走（千分位级低于噪声底，以紧邻交替六 fork 的 44 共享用例零回归入册）；双侧同窗处置时段型污染的方法学留档。

23. **第十九轮（R31·启动装载并行化，2026-08-27，详见 [report/perf/2026-08-27-startup-load-parallel.md](report/perf/2026-08-27-startup-load-parallel.md)）**：ShopLoader.loadShops 读店循环「supplyAsync 后逐店 join」的伪异步串行流水（CPU 核数级线程池退化为单线程+每店移交开销）改为全量收集 futures 后 allOf 聚合等待——计数器/列表/异常隔离/nextTick 时序全保持，仅日志交错真实并行化；2000 店全链 wall-clock **-65.9%**（98ms→33ms，六 fork 零重叠），`database.loader-threads=1` 可完全复原旧行为。新增 startup/shopLoadChain wall-clock 计量面。基准 46 用例。 **补完（同日）**：启用链路三方面闭环——II 语言包相位：fallback 110KB 双解析改单次复用（fillMissing 只读 fallback 已证；构件单价 ≈4.6ms/次，每启用/reload 省 P），bundled zip 扫描甄别为**零条目无可优化面**（仓库 lang/ 无子目录翻译，多语言走 Crowdin）；III 装配序列甄别入册（tagManager 同步契约保留、initDatabase 必要前置、重负载项已异步）；text 新增 fallbackYamlParse 单价计量面，round31b 六 fork 同码零回归佐证。基准 47 用例。
24. **第二十轮（R32·监听与展示物路径热快照清扫，2026-08-28，详见 [report/perf/2026-08-28-listener-display-config-snapshot.md](report/perf/2026-08-28-listener-display-config-snapshot.md)）**：R30 清扫法的延续——AbstractDisplayItem getNowUsing/createGuardItemStack/getDisplayLocation 三 config 行走改静态快照（refreshConfigSnapshots 挂 Util.initialize reload 钩子，null/NaN 哨兵保留启用前原语义）、VirtualDisplayItemManager allowEnchants/useItemName 快照化并补挂 Reloadable、ShopProtectionListener/PlayerLockClickListener/BlockListener 五键监听快照、Util.parse 价格后缀 Pattern 类常量化、SimpleTextManager.postProcess 流式改索引拷贝；listener/inventoryCheck **-69.0%**（八 fork 零重叠，VIRTUALITEM 门从 config 行走变静态读）、chunkLoad -24.1%，47 共享用例零回归。新增五类 17 条快照语义用例（合计 164）。**方法学留档**：快照时代基准夹具的「晚置配置」陷阱——ListenerBench 在 Util.initialize 后才 setConfig(display-type) 使候选测到哨兵路径并跨套件污染分配状态（20-200× 假回归），处置为 Env.install 前置注入 + 删数据重测（4 fork/侧）。
25. **第二十一轮（R33·日志管线热路径零成本化，2026-08-28，详见 [report/perf/2026-08-28-log-pipeline-zero-cost.md](report/perf/2026-08-28-log-pipeline-zero-cost.md)）**：被前二十轮绕过的 Log 漏斗——全局 RW 写锁+EvictingQueue 改无锁定容环形（AtomicLong 游标+槽位 seq 配对，容量语义不变，fetchLogs 弱一致仅喂 dev 查看器），Record 懒消息（Supplier 首读求值记忆，String 路径字节不变），热点 11 调用点转懒构（可变字段先快照=记录值与急切形式逐字节一致），PerfMonitor.close 无限额零时钟读，Caller 禁用开关 sysprop 类常量化；log 追加直测 **-13.4%**（近分离）、economy/safeCommit **-11~-14.5%**、inventoryTxCommit -6.1%（最优 fork -21%），49 共享用例零回归。新增 log 套件两用例（49×9）与 LogRingAndLazyMessageTest 六用例（合计 170）。

26. **第二十二轮（R34·库存操作层写入时变更日志化，2026-08-29，详见 [report/perf/2026-08-29-inventory-op-journal.md](report/perf/2026-08-29-inventory-op-journal.md)）**：AddItem/RemoveItemOperation 每 commit 的全量 createSnapshot（≈95 次逐槽 NBT 深克隆/交易，成功时从不使用）与 removeItem 逐命中整组 setStorageContents 写回，重构为写入时槽位级 MutationJournal（被触槽位一次 getAmount 记账、capture 冻结、restore LIFO 重放）+ BukkitInventoryWrapper addItem/removeItem 槽位级镜像（addItem 逐行镜像 CraftBukkit 上游算法、removeItem 镜像接口默认算法，语义均不变；第三方包装器默认走原快照路径纯增量兼容）；inventoryTxCommit **-36.4%**（八 fork 完全分离）、tradeServiceBuy/Sell **-10%**、actionSell **-13.2%**，49 共享用例零回归。新增 InventoryMutationJournalTest 九用例（合计 176）与基准剖析模式（`-Dbenchmark.profile`）。**方法学留档**：v1 的 begin/capture 双次逐槽 getAmount diff 在 mock 计量面放大成 +41% 假回归——凡新增逐槽 Bukkit-API 循环必须先过 mock 放大审查（一次 mock 调用 ≈ 生产的 1000 倍）。

27. **第二十三轮（R35·扫描层跨类型预闸与逐槽解析提升，2026-08-29，详见 [report/perf/2026-08-29-scan-pregate-hoist.md](report/perf/2026-08-29-scan-pregate-hoist.md)）**：R34 后剖析定位剩余大头＝交易预览的两次全库存扫描（每槽走完整 matcher：isSimilar→事件闸→shopId→typeMatches）；QuickShopItemMatcherImpl 顶部加跨类型预闸（无监听器且原型 shopId 为空时材料不同直接 false，原型 Material/shopId 走单条目缓存；等价论证：跨类型仅事件与 shopId 两条出路），ContainerShop 新增 countStockItems/countStockSpaces 快速计数（matcher/原型/单位大小/最大堆叠每扫描一次，第三方 matcher 分支即原泛型循环），Util 分发（CountableInventoryWrapper 优先级不变）；countItemsScan54 **-43.9%**、countSpaceScan41 **-47.4%**、tradeServiceBuy/Sell **-36.7%/-37.4%**、actionBuy/Sell **-29.1%/-32.5%**——**六用例八 fork 完全分离**，其余 43 用例零回归。ContainerShopMatchesTest 增三用例（合计 179）。

28. **第二十四轮（R38·分配轴计量与重复调用消除，2026-09-05，详见 [report/perf/2026-09-05-alloc-axis-dedup.md](report/perf/2026-09-05-alloc-axis-dedup.md)）**：R36 安全审计与 R37 功能裁剪后重启性能循环；时序轴残余 ~50% 为 Mockito 栈噪声，故启用 **B/op 分配轴**（ThreadMXBean 采样边界快照，近乎确定性，`-Dbenchmark.alloc`）+ JFR ObjectAllocationSample 全栈归因（"首个 quickshop 业务帧"剥离 mock 噪声）。五处观察等价改写：MarketUtils STOCK 排序装饰法（比较器每次比较重读排序键→每商店恰读一次）、事务日志描述符急切求值×3 改引用快照懒求值、`getItem()` 剔除从未派发的 ShopItemEvent(RETRIEVE) 死包装、countStockItems 无监听时跳过冗余 AIR 门 getType、RemoveItemOperation 循环外单次克隆；countItemsScan54 **-29.1%**、browseStockPipeline **-19.6%**、tradeServiceBuy **-11.3%**、actionBuy/Sell **-9.0%/-7.2%**（六用例 B/op 完全分离），其余 38 用例 +0.0% 逐位零回归；179 用例全绿。留档 R39 线索：ContainerShop 全字段 @EqualsAndHashCode 使 `loadedShops.contains` 付出深哈希/深等值整负载遍历（runtimeUuid 查找 30,312 B/op 的主导项）。

29. **第二十五轮（R39·商店哈希契约修复与世界过滤快照，2026-09-05，详见 [report/perf/2026-09-05-shop-hash-contract.md](report/perf/2026-09-05-shop-hash-contract.md)）**：JFR 定向归因拿到 runtimeUuid 查找的完整证据链——Lombok @EqualsAndHashCode 默认**经 getter 哈希字段**，而 ContainerShop 的 getter 是事件包装（构造并 callEvent RETRIEVE 事件）或防御克隆，旧 hashCode() 单次＝数次 Bukkit 事件派发＋isPrimaryThread＋两次 NBT 克隆，由所有商店 Set/Map 成员操作支付（每笔交易的签名排程去重、runtimeUuid 解析、装载/卸载）；且可变字段入哈希致桶漂移（SignUpdateWatcher.pendingShops 幽灵条目/去重失效隐患——其注释自 R31 起假设的身份语义实际不成立）。手写稳定 hashCode（final location 键，equals 未动，契约自洽，javap 验证 Lombok 让位）+ Util.isBlacklistWorld 白/黑名单快照化（原每次调用物化两个配置列表，位于 isValid→canBeShop 热路径）；runtimeUuid 查找 **-66.1%**（30,312→9,624 B/op，时序 -57~-60%）、tradeServiceBuy/Sell **-5.8%/-5.6%**、actionBuy/Sell **-5.0%/-4.9%**（B/op 完全分离），其余 43 用例区间全重叠零回归；metricInsertBatch100 -3.5% 经同码跨会话对照甄别为环境方差如实剔除。新增 6 用例（哈希跨变更稳定+成员关系存活、有监听器时 hashCode 零派发、equals 契约、快照零配置读取/白名单优先/reload 刷新），合计 **185 全绿**。R38+R39 累计：runtimeUuid **-68.2%**、countItemsScan **-29.1%**、browseStock **-19.6%**、tradeServiceBuy **-16.4%**。

30. **第二十六轮（R40·库存定位链成本剥离，2026-09-06，详见 [report/perf/2026-09-06-locate-chain-cost.md](report/perf/2026-09-06-locate-chain-cost.md)）**：JFR 复查定位到 `shop.getInventory()` 每次都要跑的 `CommonUtil.isJson(symbolLink)`——**对链接串做全量 Gson 解析仅为判别格式，而生产占绝对多数的新格式链接 `v;x;y;z;world` 必然解析失败**，即每次库存定位都付一次解析器缓冲分配 + JsonSyntaxException 构造与栈回填（JFR：trade 套件 isJson 帧 27.8 MB 采样；fillInStackTrace 出现在 tradeServiceBuy 叶帧）。改为首字符判别（旧格式 Gson 对象必以 `{` 开头、新格式必以数字开头，路由对内部两种格式严格一致，由基线事实闭环）；附带 PerfMonitor 计时基 Instant.now()→System.nanoTime()（每次受监测块省一个 Instant 分配，消息文本/告警级逐位保持，限额判定免疫 NTP）。新增基准用例 trade/locateSymbolLink 隔离计量定位链全成本（基线 worktree 仅同步基准模块单文件保持两侧同名单）；locateSymbolLink **-29.8%**（16,104→11,312 B/op，fork 完全分离）、actionBuy/Sell **-1.2%**、tradeServiceBuy/Sell **-0.4%/-0.5%**（均完全分离，分配量与每 op 定位次数自洽：service 1 次/action 约 3 次），其余 45 用例区间全重叠零回归（lookup 系 +1.2~2.0% 系已知抖动带、fallbackYamlParse 双峰翻转，均如实留档）。新增 3 用例（新格式 BlockPos 路由、旧格式 Gson 路由、损坏输入保持 IllegalArgumentException），合计 **188 全绿**、基准 50 用例。R38–R40 累计：runtimeUuid **-67.9%**、countItemsScan **-29.1%**、browseStock **-19.6%**、tradeServiceBuy **-16.9%**、actionBuy **-13.9%**。

31. **第二十七轮（R41·浏览菜单物品访问成本，2026-09-06，详见 [report/perf/2026-09-06-menu-item-access.md](report/perf/2026-09-06-menu-item-access.md)）**：menu 套件逐用例剖析后代码走查揭示——`shop.getItem()` 是防御克隆（生产为 NBT 深拷贝）而浏览菜单大量「克隆只为读类型」：分组主页 `groupShopsByItem` 每店 2+N 次克隆（类型门+每同材质组探测+建组，且 `getRepresentativeItem()` 每调用再克隆，200 店 8 材质 ≈1600+ 次深拷贝/页）；NAME 排序比较器**每次比较**重派生键（克隆+prettifyText，≈2·n·log n 次——R38 修 STOCK 时遗漏的同病）；页面构建五处 `getItem().getType()` 纯类型读取；`getItemDisplayName` 最多三次元数据克隆。ShopMeta 接口新增 `getMaterial()` 默认方法（与 `getItemUnitSize` 同型免克隆读取契约，ContainerShop 既有字段直读实现自然覆写）+ 分组循环单克隆复用 + 包私有免克隆代表物读 + NAME 装饰排序法与 Material 域 memoize（排序语义逐位保持）+ 页面五处改 `getMaterial()`。新增基准用例 menu/groupAndNameSort（真实 matcher 分组+NAME 排序；Shop mock 桥接 default answer 使单一基准源两侧同测各侧真实管线）；**-52.5%**（10,203,792→4,842,868 B/op，fork 完全分离，时序 -56.0%），其余 50 共享用例逐位 +0.0% 或区间重叠零回归（lookup 抖动带与 fallbackYamlParse 双峰反向翻转如实留档）。新增 2 用例（分组等价+每店 getItem 恰 1 次调用数契约、NAME 排序与历史比较器逐位等价+等键稳定序），合计 **190 全绿**、基准 51 用例。
32. **第二十八轮（R42·保存路径物品编码 memo，2026-09-06，详见 [report/perf/2026-09-06-save-encode-memo.md](report/perf/2026-09-06-save-encode-memo.md)）**：`createDataRecord`（每次脏店冲刷）与 `saveToInfoStorage`（每笔购买的日志冲刷快照）每次都跑 `platform.encodeStack`（生产为 NBT 全量拷贝+Base64，~10-50μs/次），而物品栈仅经 setItem 换引用与 allow-stack 数量翻转变化——新增双槽 (引用,数量)→编码 memo（transient+@EqualsAndHashCode.Exclude）；同时修复上游 setItem 裸引用别名缺陷（构造器克隆而 setItem 不克隆，玩家手持活栈的外部原位突变可渗入商店状态）改为与构造器一致的防御快照。createDataRecord **-14.2%**、insertShopChain **-11.9%**、updateShop-unchangedData **-14.1%**（三用例 B/op fork 完全分离；插入链编码次数减半、不变跳写路径的对照构建也变廉），其余 48 用例区间全重叠零回归（metricInsertBatch100 +1.1% 依 R39 先例甄别为 H2 跨会话漂移、fallbackYamlParse 双峰反向翻转，如实留档）。新增 2 用例（同栈态重复保存恰一次编码+值稳定、setItem 换栈重编码再命中），合计 **192 全绿**。R38–R42 累计：runtimeUuid **-68.3%**、groupAndNameSort **-52.5%**、countItemsScan **-29.1%**、locateSymbolLink **-29.8%**、tradeServiceBuy **-16.9%**。
34. **第三十轮（R44·经济提交路径 provider 解析链收敛，2026-09-07，详见 [report/perf/2026-09-07-economy-provider-resolution.md](report/perf/2026-09-07-economy-provider-resolution.md)）**：剖析模式定位（safeCommit 顶帧 QuickShopAPI.getInstance 770 采样）——EconomyWithdraw/DepositOperation 的 commit/rollback 每次都跑 `QuickShopAPI.getInstance().getEconomyManager().provider()` 完整服务定位链（每笔交易 2-3 次、回滚再计），而交易构造时已解析并持有 provider；新增 provider 参透构造器重载（transient，旧三参构造器 null 回退保持逐次解析的 API 兼容），交易五处操作构造点传入自身 provider——转账与授权余额检查严格同 core，顺带封堵理论上的检查(X)转账(Y)跨 provider 半转账窗口；QSEconomyManager.useProvider 存储时归一化大写、provider() 收敛为裸 map get（省每调用 toUpperCase 分配）。safeCommitNoTax **-76.1%**、safeCommitWithTax **-82.0%**（B/op 基线三 fork 逐位一致、候选完全分离；mock 放大如实披露——生产每链 ~6 调用+两映射查找）、actionBuy/Sell **-1.9%** 双双完全分离（模型自洽：WithTax 比 NoTax 多移除一条链），其余 49 用例零回归（listShops/shopLoadChain 漂移带区间相接、粒度级偏移与 ns 噪声如实留档）。新增 4 用例（注入 provider 零服务链调用契约、旧构造器保持链解析、交易操作构造时 provider 不因管理器事后切换改道、useProvider 大小写归一化等价），合计 **205 全绿**。
36. **第三十二轮（R46·权限节点与设置事件读取路径清扫，2026-09-08，详见 [report/perf/2026-09-08-permission-node-retrieve-events.md](report/perf/2026-09-08-permission-node-retrieve-events.md)）**：剖析定位（createDataRecord 顶帧 QuickShopAPI.getPluginInstance 469 采样）——BuiltInShopPermission(Group).getNamespacedNode 每次调用都跑服务定位链+toLowerCase+拼接（位于每次商店权限检查路径：getPlayerGroup→getPermissionAudiences→节点解析），插件名每运行期恒定可一次构建；ContainerShop 七处 RETRIEVE 设置读取（getPlayerGroup/shopState/shopType/getSignText/getTaxAccount/isDisableDisplay/店主名）每次都构造事件对象（ShopSettingEvent 构造即 updated=old、callEvent 已有 hasListeners 派发门、无监听器时 updated() 必返原值，构造是纯预付）。两枚举加惰性 namespacedNode 记忆字段（良性竞争只重建相等串）；七处读取 hasListeners 门控（门内即原代码，第三方监听器在时行为逐位不变）。createDataRecord **-57.6%**、updateShop-unchangedData **-53.7%**、insertShopChain **-45.7%**（三用例 B/op fork 完全分离，ns 轴 -62/-51/-25% 同向，三用例消减量与「每保存冲刷 3 事件+1-2 节点链」模型自洽）、actionBuy/Sell -0.2% 分离（交易路径权限/设置读取摊薄），其余 48 用例零回归（listShops/shopLoadChain H2 漂移带、metricInsertSingle/groupAndNameSort 粒度带、signRender 不经门控入口不受益亦不回归，均如实留档）；mock 放大如实披露（生产每次保存实省 3 小事件+1-2 服务链）。新增 4 用例（无监听器回退原值、监听器在时派发+覆写生效并随注销恢复、税账号覆写、节点记忆同实例至多两链），合计 **221 全绿**。
35. **第三十一轮（R45·文本渲染管线收敛，2026-09-08，详见 [report/perf/2026-09-08-text-render-pipeline.md](report/perf/2026-09-08-text-render-pipeline.md)）**：剖析定位（forLocaleWithArgs 主导帧 MsgUtil.fillArgs 48%、walk/splitTextNode 20%、findRelativeLanguages 双调）——预解析渲染成功后 FillerProcessor 仍对成品树逐参 replaceText（每参一个 TextReplacementConfig 分配+全树遍历，占位符已消费、匹配可证零命中），walk/split 每渲染重推导与参数无关的拆分骨架（未变路径也付 ArrayList）。MsgUtil.fillArgs 新增全树 '{' 预扫快速路径（扫描域=replaceText 递归域超集：文本内容+可译组件 Component 参数+hover 值+children，已对 adventure 4.16 反编译核实；无 brace 跳过全部 config 与遍历仅留 compact，嵌套 {N} 再填语义用例钉住）；PreParsedTemplate 重写为解析期编译模型（哨兵拆分/片段/槽位冻结为 sealed PermNode 骨架，parse 以哑元参数跑一次完整 render+containsSentinel 冻结参数无关回退为 BROKEN，render 只验证+装配，字面片段跨渲染共享）；TextList.forLocale 双调单次化；preParsedTemplateCache 改嵌套 locale→path 免键拼接。开发中抓获等价陷阱：MiniMessage 连续样式段产嵌套树，深处哨兵骑在前一 Split 的原始子树上不可达拆分，历版靠 containsSentinel 兜底每渲染回退——首版编译漏跑该验证被语料测试当场暴露（2 红），补齐后全过。forLocaleWithArgs **-44.0%**（2,944→1,648 B/op fork 完全分离，ns -22.1% 同向）、signRender **-0.6%**（fork 完全分离，签名价格行同管线自洽），其余 51 用例零回归（getShopByLocation 56B 粒度漂移带有同码跨会话佐证：R44 候选与 R45 基线同码，round44 测 1,992、round45 基线 1,936-1,968、候选 1,992；runtimeUuid/safeCommitWithTax 粒度带、fallbackYamlParse 双峰如实留档）。本轮消除主体为 adventure 库对象与 JDK 集合分配，生产等量、非 mock 面。新增 12 用例，合计 **217 全绿**。
33. **第二十九轮（R43·方块状态快照门控清扫，2026-09-07，详见 [report/perf/2026-09-07-block-state-snapshot-gates.md](report/perf/2026-09-07-block-state-snapshot-gates.md)）**：反编译实证 Paper 1.21.11 的 `Inventory#getHolder()` 每次调用都做方块实体全量快照拷贝（漏斗 5 槽/箱子 27+ 槽物品清单，`Container.getOwner→BlockEntity.getOwner(true)→CraftBlock.getState(true)→createSnapshot`），而 `getHolder(false)` 活状态免拷贝——挂在全服级事件上的四处「快照只为 instanceof」同族清扫：漏斗/投掷器 `InventoryMoveItemEvent` 两处理器门控（技术服每秒数百次搬运×2 次 BE 快照，与商店无关也照付）改活状态判断（BlockListener:163 既有先例），owner-exclude PDC 罕见分支保留快照 holder 语义；`onPlaceProtectedBlock` 与 `LockListener.onSignPlace`（每次方块放置触发）分别加 Material 门与静态 EnumSet 告示牌材质门（漏斗/投掷器放置从 2 次 getState 降为 1 次）。新增基准 listener/hopperMoveGate 与 hopperMoveProtect（1KB BE 快照成本模型，R24 先例；一个 op=一个事件过两处理器）：**-9.6%/-8.6%** B/op（基线三 fork 逐位一致、候选完全分离），其余 51 用例零回归（createDataRecord +0.4% 为同码路径无因果的共享 JVM 预热扰动、候选 fork2/3 粒度级偏移带 fork1 逐位一致、listShops H2 漂移带，均如实留档）。新增 9 用例（活门控零快照变体调用契约、owner-exclude 读快照 holder 的 PDC 语义证明、放置/告示牌门跳过与写入保持），合计 **201 全绿**、基准 53 用例。

15. **第十一轮（R23·浏览菜单库存快照批量化，2026-08-26，详见 [report/perf/2026-08-26-menu-inventory-snapshot.md](report/perf/2026-08-26-menu-inventory-snapshot.md)）**：菜单库存读取此前主线程必抛 IllegalStateException 被吞恒返 0（库存显示/有货过滤/库存排序三功能损坏）且每店一次阻塞往返×comparator 重查；改为整页一次「冲刷+IN(...) 批量查询」快照 Map（DatabaseHelper.queryInventoryCaches 新 API，MarketUtils 全链 Map 重载），并修复三处浏览页分页重叠缺陷（start 按 9 推进而每页 36 项）。基准 **-49.5%**（六 fork 零重叠）。测试 116 用例全绿、基准 38 用例。

14. **第十轮（R22·展示物区块进入路径去冗余，2026-08-26，详见 [report/perf/2026-08-26-display-resend-dedup.md](report/perf/2026-08-26-display-resend-dedup.md)）**：五个包工厂的「显式 destroy + sendFakeItem（内部又以 destroy 开头）」统一为 manager 单入口 resendChunkDisplays/withdrawChunkDisplays（桶锁外发包），每玩家每店少 1 包+1 事件（基准 **-21.1%**、六 fork 零重叠）；顺带修复「后端启用失败 → 插件整体崩溃」稳定性缺陷（setHandler 仅选已启用后端 + Throwable 级内存降级）并补齐 R21 实机验证（packetevents 2.13.1-SNAPSHOT 下 DISPLAY-CHECK PASSED）。测试 110 用例全绿、基准 37 用例。

13. **第九轮（R21·CHUNK_DATA 监听免全量解析 + 基准根因修复，2026-08-18，详见 [report/perf/2026-08-18-chunk-packet-peek.md](report/perf/2026-08-18-chunk-packet-peek.md)）**：展示物区块包监听从 WrapperPlayServerChunkData 全区块逐段解析（Netty 线程、每玩家每包几十~几百 KB）改为头部两 int 的 O(1) peek（三个 packetevents 工厂）；顺带破案修复基准 harness 的 Mockito 调用记录泄漏（R20"text 污染"真因，报告已更正归因），A/B 方差从 ±15~45% 收敛至 ±1%。测试 107 用例全绿、基准 36 用例。
12. **第八轮（R20·购买日志卸载，2026-08-18，详见 [report/perf/2026-08-18-purchase-log-offload.md](report/perf/2026-08-18-purchase-log-offload.md)）**：LogWatcher 懒队列 + `logEventLazy` + 监听器懒构造——每笔交易日志序列化移出主线程（**-97.9%**，180μs→3.7μs，5v5 fork 零交叉）；qs.log 内容/顺序/轮转不变。测试 104 用例全绿、基准 35 用例；八轮累计交易全链约 **-84%**。
11. **第七轮（R19·带参文本预解析，2026-08-18，详见 [report/perf/2026-08-18-text-preparse.md](report/perf/2026-08-18-text-preparse.md)）**：占位符模板预解析为哨兵孔组件树，参数组件直插（免序列化与全模板重解析），带参渲染 27μs→1.1μs（**-95.9%**）；三重守卫不满足即回退原路径，内置 282 模板全语料逐字节等价。测试 100 用例全绿。
10. **第六轮（R18·收据/通知单次 getItem，2026-08-17，详见 [report/perf/2026-08-17-receipt-item-dedup.md](report/perf/2026-08-17-receipt-item-dedup.md)）**：收据与店主通知各取一次商店物品并复用（printEnchantment 增传栈重载），每笔交易省 4~6 次克隆与 RETRIEVE 事件；actionBuy 再 -8.7%。会话五轮总览见该报告末尾（交易全链买 -82%、每笔 DB 写全批量化）。
9. **第五轮（R17·剩余每笔交易 DB 写批处理，2026-08-17，详见 [report/perf/2026-08-17-remaining-db-write-batching.md](report/perf/2026-08-17-remaining-db-write-batching.md)）**：通用 `BatchingQueue`（按键保末值去重）+ `DbWriteBatcher`——qs_external_cache 批量 REPLACE（每条摊销 **-67.7%**，同店连点 N 行→1 行）与 qs_messages 离线消息保序批插；读方链式冲刷保精确新鲜。每笔成功交易的 DB 写全部批量化（指标/缓存/消息各 1/窗口），主线程提交成本百 ns 级。测试 91 用例全绿、基准 34 用例。
8. **第四轮（R16·DB 写批处理与扫描未命中路径，2026-08-17，详见 [report/perf/2026-08-17-db-batch-and-matcher-optimization.md](report/perf/2026-08-17-db-batch-and-matcher-optimization.md)）**：匹配器 shopId 源查询身份缓存（扫描 -17.6%/-20.7%，交易全链再 -13~16%）+ 每笔交易指标插入 MetricBatcher 批处理（H2 每条摊销 -32.6%，主线程提交降为队列入队；`insertMetricRecords` 默认方法保持 API 兼容）。测试 87 用例全绿、基准 32 用例。三轮累计买/卖全链 -83.3%/-82.8%。
7. **第三轮（action 层全路径，R15，2026-08-17，详见 [report/perf/2026-08-17-action-path-optimization.md](report/perf/2026-08-17-action-path-optimization.md)）**：玩家侧交易入口的预检重复扫描消除（预检测量经 `TradeResult.observation()` 透出，API 加法兼容）、匹配器未命中路径类型门（每异类槽省 2 克隆）、卖方向冗余木牌立即渲染移除（R10 遗漏点）。actionBuy/actionSell **-73.0%/-66.7%**（6 fork 无交叉），两轮累计买/卖全链 -80.5%/-79.5%。测试 80 用例全绿、基准 30 用例；顺带修复基准 mock World 弱引用 GC 隐患。

## 笔记目录

| 笔记 | 内容 |
|---|---|
| [01-架构.md](01-架构.md) | Maven 模块划分、技术栈、核心类关系、数据库表、12 种设计模式 |
| [02-运行原理.md](02-运行原理.md) | 启动流程、数据库初始化与迁移、商店加载、交易时序、线程模型、状态机、关停 |
| [03-工作流.md](03-workflow.md) | 建店/交易/删店业务流程、CI/CD、异常恢复、测试现状 |
| [04-开发速查.md](04-开发速查.md) | 关键代码位置索引、已知缺陷、扩展开发指南（子命令/兼容模块/迁移） |
| [05-安全审计-R36.md](05-安全审计-R36.md) | R36 安全与稳定性审计（2026-08-29）：六维度扫描+七批次修复清单、交易回滚不变式、已知残留与理由、新增权限节点 |
| [06-功能裁剪-R37.md](06-功能裁剪-R37.md) | R37 功能裁剪（2026-09-01）：八类功能移除（VaultUnlocked/多币种/联网更新检测与遥测/Crowdin OTA 本地化仅中文/Bungee 跨服/6 addon/14 保护 compat）、联网行为边界、DB schema 保留策略、验证结论 |

## 路径缩写约定

以下笔记中：`QS/` = `quickshop-bukkit/src/main/java/com/ghostchu/quickshop/`，`API/` = `quickshop-api/src/main/java/com/ghostchu/quickshop/api/`。
