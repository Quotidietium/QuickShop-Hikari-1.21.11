# 04 · 开发速查

> 面向在本 fork 上继续开发的快速索引。路径缩写：`QS/` = `quickshop-bukkit/src/main/java/com/ghostchu/quickshop/`，`API/` = `quickshop-api/src/main/java/com/ghostchu/quickshop/api/`

## 1. 关键文件索引

| 要找什么 | 位置 |
|---|---|
| 插件入口/平台检测/libby 装库 | QS/QuickShopBukkit.java |
| 服务定位器核心（40+ 管理器装配、onEnable 42 步） | QS/QuickShop.java（1447 行） |
| 商店实体（物品/价格/告示牌/展示物/状态） | QS/shop/ContainerShop.java（2036 行） |
| 交易入口/建店/删店/聊天价格 | QS/shop/SimpleShopManager.java（1632 行，含 AbstractShopManager 基类的查找表） |
| 买卖四方法门面 + 预检 | QS/shop/SimpleTradeService.java |
| 库存事务（Operation 栈/快照回滚） | QS/shop/SimpleInventoryTransaction.java |
| 经济事务（税计算/补偿回滚） | QS/economy/transaction/QSEconomyTransaction.java |
| 经济 provider（Vault/VaultUnlocked） | QS/economy/provider/ |
| 经济延迟装载 | QS/economy/EconomyLoader.java + QS/listener/EconomySetupListener.java |
| 玩家点击路由（125ms 限流/双箱规则） | QS/listener/PlayerListener.java |
| 交互类型→行为映射（interaction.yml） | QS/shop/interaction/ |
| DB DAO + 迁移链（版本 20） | QS/database/SimpleDatabaseHelperV2.java |
| 12 张表 DDL 枚举 | QS/database/DataTables.java |
| 商店并行加载 | QS/shop/ShopLoader.java |
| 虚拟展示物（PacketEvents/ProtocolLib） | QS/shop/display/virtual/ |
| 60 个子命令 | QS/command/subcommand/（silent/ 下是 silentbuy 等特殊命令） |
| 命令注册点 | QS/command/SimpleCommandManager.java（构造器内逐一 registerCmd） |
| 线程池定义 | quickshop-common/.../util/QuickExecutor.java |
| i18n / Crowdin OTA | QS/localization/SimpleTextManager.java |
| 主配置 | quickshop-bukkit/src/main/resources/config.yml |
| 兼容模块基类 | compatibility/common/.../CompatibilityModule.java |
| 控制面板组件 | QS/shop/controlpanel/component/（12 个，全实现 ControlComponent） |
| 性能基准模块（49 用例 × 9 套件） | benchmark/（独立 Maven 模块，`mvn -q compile exec:exec -Dbenchmark.label=<标签> -Dbenchmark.fork=N`；涉配置用例必须在 `Env.install()`→`Util.initialize()` 前布置配置，否则测到快照哨兵路径） |
| 基准对比工具 | benchmark/src/main/java/com/ghostchu/quickshop/benchmark/Compare.java（按目录聚合 fork 中位数输出 markdown） |
| 性能报告·第一轮（R2–R7） | note/report/perf/2026-08-16-performance-optimization.md |
| 性能报告·第二轮（R8–R12 交易热路径） | note/report/perf/2026-08-16-trade-path-optimization.md |
| 性能报告·第三轮（R15 action 层全路径） | note/report/perf/2026-08-17-action-path-optimization.md |
| 性能报告·第四轮（R16 DB 批处理与匹配器 shopId 缓存） | note/report/perf/2026-08-17-db-batch-and-matcher-optimization.md |
| 性能报告·第五轮（R17 剩余 DB 写批处理） | note/report/perf/2026-08-17-remaining-db-write-batching.md |
| 性能报告·第六轮（R18 收据单次取数 + 五轮总览） | note/report/perf/2026-08-17-receipt-item-dedup.md |
| 性能报告·第七轮（R19 带参文本预解析） | note/report/perf/2026-08-18-text-preparse.md |
| 性能报告·第八轮（R20 购买日志卸载） | note/report/perf/2026-08-18-purchase-log-offload.md |
| 性能报告·第九轮（R21 区块包 peek + 基准泄漏修复） | note/report/perf/2026-08-18-chunk-packet-peek.md |

## 2. 缺陷修复记录（2026-08-16 审计循环，commit 472e773bb..3842651ac 及后续）

以下缺陷已在审计循环中全部修复（每项独立提交，可单独回滚）：

| # | 缺陷 | 位置 | 修复 commit |
|---|---|---|---|
| 1 | checkTax 守卫写反 `totalTax>0→return`，正税额从不入账（账实不符） | QSEconomyTransaction.java checkTax | 472e773bb |
| 2 | 经济 commit 插件取消/扣款失败分支漏调 onFailed | QSEconomyTransaction.java commit | e19f7407d |
| 3 | 负数/损坏收益份额可放大店主分成（潜在印钱）；to==null 时无限商店+分成组合必失败 | QSEconomyTransaction.java benefit 循环 | 94b877cb0 |
| 4 | 库存 commit 插件取消分支漏调 onFailed | SimpleInventoryTransaction.java | 8d71e34ef |
| 5 | **交易数量溢出/零单位无防护**：unitSize=0 或 unitSize×amount 溢出为 0 时移 0 件物品却全额转账（无限商店=无中生有刷钱） | SimpleTradeService.java | 9c88938d5 |
| 6 | 聊天交易数量 parseInt 对超长数字抛未捕获 NFE（区域线程） | SimpleShopManager.actionTrade | 0fbfe453d |
| 7 | 价格过小提示显示 getMax() 而非 getMin() | SimpleShopManager.createShop | 3842651ac |
| 8 | 子命令 8 处未捕获 parseInt NFE（页码 avoid/favorite/tag×3/watch、refill 数量、debug 连接池设置） | subcommand/* + Util.parseIntegerSafely | a785c0e94 |
| 9 | **totalTax 用了税率参数而非换算后金额**（缺 this. 前缀）：640 元 5% 税只入账 0.05 而非 32.00，差额凭空蒸发——由回归测试的金额守恒断言抓获 | QSEconomyTransaction 构造器 | de9252aba |
| 10 | **商店税号加载即丢失**：`this.taxAccount = getTaxAccount()` 调的是刚置 null 字段的 getter，随后保存把 null 写回库（永久数据丢失） | ShopLoader.DataRawDatabaseInfo | 0e34ba806 |
| 11 | **离线消息 flush 竞窗口丢信**：select→send 后按玩家全删，窗口内新交易消息被静默删除；改为按 flush 起始时间戳删除 | MsgUtil.flush + cleanMessageForPlayer(uuid,time) | 3cf243068 |
| 12 | inShop 队列泄漏×2：InventoryClose 两个绕行分支跳过 remove；锁定商店点击取消后仍入队（无 Close 事件清理）——无限队列+O(n) 扫描劣化 | PlayerListener / PlayerLockClickListener | 6cdc036bb / 5a76e2535 |
| 13 | SpaceCache 构造参数对调（stock/space），去重比较永不相等→每次库存计算事件都写库（高负载写风暴） | InternalListener | 3b2fe6204 |
| 14 | limited 插件两处参数缺失 IOOBE 崩溃（limit set/period 无值）；日历重置在异步线程改 extra（YAML 非线程安全，与交易/保存并发） | addon/limited | 56b3f209d |
| 15 | list 插件页码 parseInt 崩溃 + name2Uuid 返回 null 直传查询 | addon/list | d26594f7b |
| 16 | PAPI 占位符天数参数×4 处 NFE——聊天插件解析玩家消息中的占位符即客户端可控输入 | PurchasesPAPI/TransactionAmountPAPI | afaea2e08 |
| 17 | quests 卖出任务校验比对的是买入任务名（复制粘贴错误）→ 配置错误时卖出被永久阻断 | addon/quests | 4f0f73380 |
| 18 | **dynmap 存储型 XSS**：商店名/物品名（玩家可控自由文本）未转义直接进入按 HTML 渲染的地图标记 | addon/dynmap | 0cebafad7 |
| 19 | **木牌更新队列跨线程竞争**：LinkedList 被区域线程 add/contains、异步定时线程 poll 共用，高负载下 CME/丢任务/链表损坏 | SignUpdateWatcher | 4c3c0d9a5 |
| 20 | **交易菜单接受负数数量**：0 值/库存/倍数三重检查均漏负数（负余数不>0），负金额进入经济栈且负数量重定向把『买』反转为『卖』 | menu/trade/MainPage | 77ccb9f77 |
| 21 | **insertMetricRecord 异常分支缺 return**：locateShopDataId 失败后仍以 null data 继续插入 qs_log_purchase | SimpleDatabaseHelperV2 | 6ab6ae9bf |
| 22 | **DataTables 仅首次绑定 SQLManager**：运行期重建连接池后所有表操作仍指向已排空的旧池（测试环境则表现为跨库串写） | DataTables.create | 6ab6ae9bf |

## 2.5 性能优化轮记录

### 第一轮（2026-08-16，R2–R7 七轮，报告见 report/perf/2026-08-16-performance-optimization.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R2 | aaae8d88b | 查找表二级索引（id/owner/runtime UUID）+ 位置探针 | getShopById 142μs→77ns；getAllShops(owner) 282μs→111ns |
| R3 | 6ab6ae9bf | DB 写路径三级缓存 + encodeStack 单次 + 两处行为修复 | updateShop 不变路径 -81%；locateShopDataId -99.6%；insert 链 -37% |
| R4 | e18a133f8 | 文本管线缓存（模板/无参组件/ProxiedLocale） | forLocaleNoArgs 18.8μs→233ns；findRelativeLanguages -77% |
| R5 | b2fb84349 | 事件零监听器快路径 | createDataRecord 累计 -51%（其中 4 个 RETRIEVE 事件免派发） |
| R6 | 5b7fa8d94 | QUser 反序列化驻留 | listShops 全量读 -38% |
| R7 | d8a70bf35 | runtime UUID 索引（回退扫描 O(loaded)→O(1)） | uncached/cached 差距 1.77×→1.04× |

### 第二轮（2026-08-16，R8–R12 + R14·交易与交互热路径，报告见 report/perf/2026-08-16-trade-path-optimization.md）

| 轮 | commit | 主题 | 代表收益（最终同态 A/B） |
|---|---|---|---|
| R8 | 91495d014 + 90a07a72e | trade 基准套件；匹配免克隆直通/事件构造守卫/事务日志免 Gson+序列化 | 扫描逐轮 -33%/-42%；事务提交 -13% |
| R9 | 2f74c964a | Bukkit 库存迭代器单次快照（O(n²)→O(n)） | 纯迭代 188μs→3.5μs（-98.1%） |
| R10 | d928b2ab1 | 交易后木牌改走 SignUpdateWatcher 批处理（配置可回退） | 渲染次数 N→⌈N/周期⌉；mock A/B -3.4%（文本/方块写入打桩低估） |
| R11 | ee56e3b39 | 买路径单次库存定位 + getItemUnitSize 免克隆读取 | 每笔少 1 次定位与 6~7 次克隆事件 |
| R12 | 49ca8f4e9 | 卖路径单次定位（getRemainingSpace/Stock 带定位重载）+ 卖出基准用例 | 卖方向定位 3→1；买卖全链路最终 -62.7%/-61.2% |
| R13 | （已回退） | 带参文本参数序列化纯文本快速路径 | 实测无收益（成本在全模板重解析）——否定性结论记录于报告，代码未保留 |
| R14 | 03a21f743 | 点击路径方块访问削减（材料预检免 getBlockData / getState(false) 免快照 / 配置快照）+ listener 基准套件 | 每次非商店点击少一次数据获取与一次快照构造；mock 增量低于噪声底，结构证明由测试承担 |

### 第三轮（2026-08-17，R15·action 层全路径，报告见 report/perf/2026-08-17-action-path-optimization.md）

| 轮 | commit | 主题 | 代表收益（同态 A/B） |
|---|---|---|---|
| R15 | 479ca5d2f..bdb2e4226 | ① `TradeResult` 附加 `TradeObservation`（预检测量透出，API 加法）② actionSelling/actionBuying 消除预检重复扫描（买：箱 2→1、背包 2→1；卖：箱 2→1）③ 匹配器未命中类型门（每异类槽免 2 克隆）④ 卖方向冗余木牌立即渲染移除（R10 遗漏） | actionBuy/actionSell **-73.0%/-66.7%**；扫描 -27%/-31%；tradeServiceBuy/Sell 再 -55%/-54% |

### 第四轮（2026-08-17，R16·DB 写批处理与扫描未命中，报告见 report/perf/2026-08-17-db-batch-and-matcher-optimization.md）

| 轮 | commit | 主题 | 代表收益（同态 A/B） |
|---|---|---|---|
| R16 | d6bd8b68f..33a8e210b | ① 匹配器 shopId 源查询身份缓存（零监听器门控）② `insertMetricRecords` JDBC 批处理 + `MetricBatcher`（10s/256 条冲刷、关停排空、读前冲刷）③ db 套件指标插入用例（批用例运行时探测注册） | 扫描 -17.6%/-20.7%；tradeServiceBuy/Sell 再 -15.9%/-14.7%；actionBuy/Sell 再 -13.2%/-11.6%；指标每条摊销 -32.6%（H2），主线程提交降为队列入队 |

### 第五轮（2026-08-17，R17·剩余每笔交易 DB 写批处理，报告见 report/perf/2026-08-17-remaining-db-write-batching.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R17 | b4c6ad872..e323f4381 | 通用 `BatchingQueue`（按键保末值）+ `DbWriteBatcher`：qs_external_cache 批量 REPLACE（同店折叠）、qs_messages 保序批插；读方链式冲刷（queryShopInventoryCacheInDatabase / MsgUtil.flush）；关停排空 | 外部缓存每条摊销 **-67.7%**（H2），同店连点 N 行→1 行；基准扩至 34 用例 |

### 第二十一轮（2026-08-28，R33·日志管线热路径零成本化，报告见 report/perf/2026-08-28-log-pipeline-zero-cost.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R33 | c4b49371b | Log 全局 RW 锁+EvictingQueue → 无锁定容环形（seq 配对槽位）；Record 懒消息；热点 11 调用点懒构；PerfMonitor.close 懒构；Caller sysprop 类常量化 | log 追加 **-13.4%**（近分离）、safeCommit **-11~-14.5%**、inventoryTxCommit -6.1%，49 共享用例零回归 |

日志管线留档（R33 留档）：
- Log 环形是弱一致读取：仅 `/qs dev` 粘贴/查看器消费，并发写进行中快照可能漏最后几条——这是入册的语义放宽点，勿用于审计场景。
- 懒 supplier 必须只读「调用时快照值」：可变字段（BigDecimal 税额/ownerPayment/QUser）与活 Map.Entry 先拷 final 局部再进 lambda，否则 dev 端晚求值会读到漂移值。
- 环内懒 supplier 持有交易对象引用直至出窗（≤14000 条），与 String 消息驻留同量级，无泄漏面。

### 第二十轮（2026-08-28，R32·监听与展示物路径热快照清扫，报告见 report/perf/2026-08-28-listener-display-config-snapshot.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R32 | 8f3633e6b..7235f9ff2 | AbstractDisplayItem 三 config 行走/VirtualDisplayItemManager 两 getter/三监听器五键 → 静态快照（reload 钩子接线，null/NaN 哨兵保留启用前语义）；Util.parse Pattern 类常量化；postProcess 索引拷贝 | listener/inventoryCheck **-69.0%**（八 fork 零重叠）、chunkLoad -24.1%，47 共享用例零回归 |

基准夹具留档（R32 留档）：
- **快照时代晚置配置陷阱**：ListenerBench 在 `Env.install()`→`Util.initialize()` 之后才 setConfig(display-type)，候选（快照化后）测到的是「快照未初始化」哨兵路径 → inventoryCheck/chunkLoad 20-200× 假回归，且全扫描分配压力跨套件污染同 JVM 同码对照（db/text 整体 +20-60% 假回归）。
- 处置：涉配置用例一律在 `Env.install()` 内 `Util.initialize()` 之前布置（display-type=2 已前置注入）；污染数据必须删除重测。
- 生产对照：config.yml 先于 onEnable 存在、变更必经 /qs reload → 快照刷新，基准此前依赖的「晚改配置立即生效」在真实服务器上不存在。

### 第十九轮（2026-08-27，R31·启动装载并行化，报告见 report/perf/2026-08-27-startup-load-parallel.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R31 | ff583e3de..d9fa43fc8 | ShopLoader.loadShops 逐店 join 串行流水 → futures 全量收集 + CompletableFuture.allOf 聚合等待（COW 列表/Atomic 计数器本为并发设计） | 2000 店启动全链 wall-clock **-65.9%**（零重叠），随 database.loader-threads 核数扩展 |

启动装载注意事项（R31 留档）：
- 「提交即 join」是伪异步反模式：用了并发设施却保留串行语义——审查异步代码先看 join/get 相对提交的位置。
- 聚合等待等价性三要素：结果容器并发安全、错误逐项隔离（exceptionally 在收集前挂）、聚合点后置动作时序不变。
- World mock 无 isWorldLoaded 方法（那是 Location 的）——桩 Util.isLoaded 只需 world.isChunkLoaded(anyInt,anyInt)=true。
- 语言包相位（R31 补完）：lang/messages.yml ~110KB 每次 load() 解析一次即可——fillMissing 只读 fallback 可共享实例；仓库 lang/ 无子目录翻译文件，loadBundled 的 zip 扫描常态匹配零条目。

### 第十八轮（2026-08-27，R30·交易链配置访问清扫，报告见 report/perf/2026-08-27-trade-chain-config-sweep.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R30 | 362c8945a..28ec82018 | actionBuying/actionSelling 复用 payUnlimitedShopOwner 快照字段 + 店主通知 notifyShowTax 独立快照（show-tax ≠ shop-tax.show）+ ShopUtil all 路径静态快照挂 Util.initialize 钩子 | 每笔交易省 2-3 次 config 树行走（千分位级，44 共享用例零回归入册） |

配置快照收尾注意事项（R30 留档）：
- 同名不同键的陷阱：`shop-tax.show` 与 `show-tax` 是两个配置项——快照化时必须核对每个调用的实际键名，勿按语义就近替换。
- 时段型污染处置：当基线取自安静窗口而候选侧同码用例也整体变慢时，「只重跑慢侧」会把环境差误判为回归——删除两侧数据改为六 fork 连续交替重测。

### 第十七轮（2026-08-27，R29·价格显示链，报告见 report/perf/2026-08-27-price-chain.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R29 | c87da46da..a9cd8f672 | EconomyFormatter/BuiltInEconomyFormatter 货币符号 reload 快照 + MsgUtil.decimalFormat 共享 DecimalFormat → ThreadLocal（区域线程并发安全） | formatInternalPrice 构件 **-98.4%**（六 fork 零重叠）；并发格式化缺陷消除 |

价格链注意事项（R29 留档）：
- `DecimalFormat` 非线程安全——任何跨线程共享实例都会在并发 format 时产生错乱输出，多线程价格渲染必须 ThreadLocal 或同步。
- 内部格式化三入口：disable-vault-format 主路径、Vault 空/异常兜底、VaultProvider.getInternalFormat 直调——改动符号逻辑需三处同验。
- ConcurrentHashMap 不接受 null value：mock 配置值需删除时用 remove，勿 put(null)。

### 第十六轮（2026-08-27，R28·杂项热路径复合，报告见 report/perf/2026-08-27-misc-hotpath-composite.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R28 | 4b9efb7de..ca0983865 | Util 物品名双标志 volatile 快照（initialize() 刷新）+ ChatListener 门控快照（init/reload 模式）+ 漏斗/投掷器监听 holder 单次解析 | itemNameFlags 构件 -99.5%（零重叠）、chatGate 中位数 -29.3%；监听器层逐调用 config 访问清零 |

杂项快照注意事项（R28 留档）：
- 无默认值 `getBoolean(path)`（缺键=false）与带默认版本语义不同——快照化时必须在 refresh 点显式传等价默认值。
- `PlayerEvent.getPlayer()` 是 final：Mockito 子类 maker 不可桩，基准环境用反射注入 player 字段；surefire 测试环境的 maker 不同、可直接桩——两处写法勿互抄。
- 被优化构件被更大共同成本（mock 调用）淹没时（全链 delta <1.3%），把基准缩到仅含被测构件再计量。

### 第十五轮（2026-08-27，R27·签名排程去重 O(1)，报告见 report/perf/2026-08-27-sign-schedule-dedup.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R27 | d3984c5ee..6f7174262 | `SignUpdateWatcher` 去重伴生 `ConcurrentHashMap.newKeySet`（ContainerShop 身份等价 → 与队列 equals 扫描语义严格一致；排空 poll-then-remove） | 漏斗供料商店重复排程去重 O(n)→O(1)（基准 -98.6%，六 fork 零重叠，无 mock 放大） |

并发去重注意事项（R27 留档）：
- `ContainerShop` 无 equals/hashCode 覆写＝身份等价——任何基于它的集合去重与线性 equals 扫描同义，可安全互换；一旦未来覆写 equals 需同步审计所有去重集合。
- Mockito 插桩 `Shop` 接口会触发其类初始化（读 `Bukkit.server`）——隔离运行单个测试类时必须先装 MockBukkit 环境（全套件运行时由先前测试类隐式装好，掩盖此依赖）。

### 第十四轮（2026-08-27，R26·区块加载路径双快门，报告见 report/perf/2026-08-27-chunk-load-fastgates.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R26 | 33c992c94..45e3d0521 | `AbstractDisplayItem.canProduceGuardItems()`（与 checkIsGuardItemStack 早退同源）门控 ChunkListener 守卫孤儿清扫——虚拟后端（默认）整段跳过 getEntities+逐实体检查；无商店区块 isEmpty 早退（修复 R2 后永不触发的死 null 检查，消除每次加载的 chunkName 拼接+PerfMonitor+空 performance 记录） | 每次区块加载 实体快照+40 检查+日志记录 → 2 次配置查询+isEmpty（基准 -95.8%，六 fork 零重叠） |

区块加载注意事项（R26 留档）：
- `getShops(world,x,z)` 三个分支均返回非 null（emptyMap 兜底）——任何「null 检查」都是死代码，应写 `isEmpty()`。
- `PerfMonitor.close()` 无条件 `Log.performance`（写锁+Record+入队）——包裹空循环的 PerfMonitor 是每次调用一条的日志噪声；放在 isEmpty 早退之后。
- 测量机承载外部负载时基准 JVM 会静默中断（exit 1/127、缓冲丢失）——以结果 JSON 的用例完整性为准，重试至安静窗口。

### 第十三轮（2026-08-27，R25·签名渲染管线，报告见 report/perf/2026-08-27-sign-render-pipeline.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R25 | c344a5f33..13653087b | `SimpleShopLayoutProvider.render` 内置买卖类型单次库存扫描共享（header 可用性与 trading 余量同源可证；第三方 IShopType 独立双查保精确）+ layout 模板按店型缓存（Reloadable 失效）+ renderItem 标志快照 + `setSignText` 每牌子 config 读取提出循环 | 每次木牌刷新 2 次全量扫描+5 次 config 查询 → 1 次扫描+缓存（基准 -47.6%，六 fork 零重叠） |

签名渲染注意事项（R25 留档）：
- `Shop#inventoryAvailable()` 与 `shopType().remainingStock(shop)` 仅对内置 Selling/BuyingType 可证同源；第三方 IShopType 的 remainingStock 可与前者不同源——共享扫描的判定边界是 `instanceof`，勿放宽。
- Mockito 陷阱（测试与基准各踩一次）：`when(mock.a()).thenReturn(new X(plugin))` 中 X 构造器若调用任一 mock 方法（如注册 reload manager），即 UnfinishedStubbingException——先构造后挂桩。
- 直跑 `java -cp` 交换类路径时勿用 shell `printf` 写前缀（`\r`/`\t` 被转义，损坏条目 JVM 静默跳过、m2 jar 兜底，A/B 变成候选对候选）；用 python 写并 Probe 验证 `getCodeSource()`。

### 第十二轮（2026-08-26，R24·开箱守卫扫描快路径，报告见 report/perf/2026-08-26-inventory-check-fastpath.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R24 | 41e7c7e64 | `Util.inventoryCheck` 在 VIRTUALITEM 模式（默认）下一次判定整段跳过守卫物槽位扫描（逐槽 checkIsGuardItemStack 恒假可证） | 每次开箱 54 次迭代+配置查询 → 1 次判定（基准 -99.6%） |

### 第十一轮（2026-08-26，R23·浏览菜单库存快照批量化，报告见 report/perf/2026-08-26-menu-inventory-snapshot.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R23 | 5bd79da2b..1fb9f3823 | `DatabaseHelper.queryInventoryCaches`（IN(...) 真批量）+ `MarketUtils.loadInventoryCaches`/`stockOf`/`spaceOf` 与全链 Map 快照重载；四处菜单页接入；三处浏览页分页 start 修复 | 库存显示/有货过滤/库存排序三功能从主线程必抛恒 0 修复为真实值；每页渲染 n 次 SELECT+整批冲刷 → 1 次（基准 -49.5%，六 fork 零重叠） |

菜单库存读取注意事项（R23 留档）：
- TNML 菜单库（BukkitInventory→runTask）**全同步主线程调度**——任何走 `queryShopInventoryCacheInDatabase` 的读取在菜单回调里必抛 `IllegalStateException`（`ensureThread(true)`）；菜单路径必须用 `MarketUtils.loadInventoryCaches` 快照。
- `Comparator.comparingInt(keyExtractor)` **不缓存键**：键提取含 I/O 时按 n·log n 次执行——排序键必须先物化。
- 浏览页分页：`start=(page-1)*items`（items=(rows-2)*9），勿再用单行 offset。

### 第十轮（2026-08-26，R22·展示物区块进入路径去冗余，报告见 report/perf/2026-08-26-display-resend-dedup.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R22 | 2994251fa..90c09fdba | 五工厂「显式 destroy + sendFakeItem」统一为 `resendChunkDisplays`/`withdrawChunkDisplays`（桶锁外发包，恰 3 包/店）；构造 clone/首扫 sqrt/死存储清理；**稳定性**：后端启用失败（如 packetevents 2.9.5 遇 1.21.11）曾致插件整体崩溃 → 仅选已启用后端 + Throwable 内存级降级 | 每玩家每店少 1 包+1 事件（基准 -21.1%，六 fork 零重叠）；实机 E2E：packetevents 2.13.1-SNAPSHOT 下 DISPLAY-CHECK PASSED |

展示物子系统注意事项（R22 留档）：
- **packetevents 运行环境要求**：需 ≥ 支持 1.21.11 的构建（2.9.5 仅到 1.21.8，启用即失败；已验证 codemc CI 2.13.1-SNAPSHOT build 907 可用）。
- **packetevents `PacketFactoryv1_21_6` 为未接线工厂**：`PacketEventsHandler` 版本表只挂 v1_20/v1_21（1.20.1–1.21.11 全由两者覆盖），v1_21_6 类存在但从未注册——若未来 packetevents 高版本需 1.21.6+ 分叉行为，先把它挂进 factories 表。
- 展示物区块进入/离开的重送与撤除统一走 `VirtualDisplayItemManager.resendChunkDisplays` / `withdrawChunkDisplays`（勿在监听器里内联重写，勿在 destroy 外再包一层 destroy——sendFakeItem 自身以 destroy 开头）。

### 第九轮（2026-08-18，R21·区块包 peek + 基准根因修复，报告见 report/perf/2026-08-18-chunk-packet-peek.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R21 | 771f303f1..9e29c5d2b | `ChunkPacketPeek`（两 int 非破坏 peek）替换三个 packetevents 工厂的全区块解析；Env.hotMock（stubOnly）修复基准 mock 记录泄漏 | 每玩家每区块包从 O(区块字节) 解析 → O(1)（Netty 线程）；A/B 方差 ±15~45% → ±1%；R20"text 污染"归因更正入册 |

### 第八轮（2026-08-18，R20·购买日志卸载，报告见 report/perf/2026-08-18-purchase-log-offload.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R20 | 849f722b2 前四笔 | LogWatcher 懒队列（同队列保序/失败隔离/close 排空）+ `QuickShop.logEventLazy` + shopPurchase 懒构造 | 每笔交易日志主线程成本 **-97.9%**（180μs→3.7μs，5v5 零交叉）；快照读取至多延后 0.5s（诊断级漂移）；text 套件本轮测量窗口污染的甄别方法（裸 JVM 复测 + 两侧同码对照）已入册 |

### 第七轮（2026-08-18，R19·带参文本预解析，报告见 report/perf/2026-08-18-text-preparse.md）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R19 | 35733c1f9..c93887c7b | `PreParsedTemplate` 哨兵孔组件树（{N}→U+E400 哨兵单次解析缓存；渲染拆节点直插参数；哨兵全消费/参数子树样式回退/异常兜底三重守卫）；Text/TextList 接入，null 回退原路径 | forLocaleWithArgs **-95.9%**（27μs→1.1μs）；actionBuy 再 -8.7%；282 模板全语料等价（TextPreParseEquivalenceTest ×9） |

### 第六轮（2026-08-17，R18·收据/通知单次 getItem）

| 轮 | commit | 主题 | 代表收益 |
|---|---|---|---|
| R18 | 8873fc9a | sendPurchaseSuccess/sendSellSuccess/notifyBought/notifySold 各单次 getItem 复用；printEnchantment 传栈重载 | actionBuy 再 **-8.7%**；每笔省 4~6 次克隆+RETRIEVE 事件 |

配套：基准 trade 套件新增 actionBuy/actionSell（R15 时 30 用例 × 7 套件），R16 扩至 32 用例，R17 扩至 34 用例；修复基准 mock World
弱引用 GC 隐患（Env.pin）；新增 TradeObservationTest ×8 + 匹配器类型门 ×2，R16 再增 MetricBatchTest ×5 与 shopId 缓存 ×2；R17 增 DbWriteBatchTest ×4；R19 增 TextPreParseEquivalenceTest ×9；R20 增 LogWatcherLazyTest ×4；R21 增 ChunkPacketPeekTest ×3，合计 **107 用例全绿**。

对应新增测试（第一轮）：ShopLookupIndexTest（7）、DbWriteCacheTest（4）、TextPipelineCacheTest（4）、QUserInternTest（1）、EventDispatchFastPathTest（2）。
第二轮新增：ContainerShopMatchesTest（4：内建匹配器免克隆语义/非突变、第三方防御拷贝、事件监听器强制匹配兼容）、InventoryWrapperIteratorTest（5：单次复制契约、setCurrent 写回、remove、越界、空库存）、TradeSignSchedulingTest（2：locale 保留与合并、null 回退 owner 语言）、PlayerListenerSearchShopTest（3：普通方块免 getBlockData、容器分类免快照、配置快照经 reload 刷新；含 Tag 静态字段经 Bukkit.getTag 桩初始化的脚手架）。合计 **70 用例全绿**。

**回归测试套件**（quickshop-bukkit 67 用例 + addon/dynmap 7 用例全绿）：H2 内存库真 SQL 测试；并发负载冒烟（12 线程×400 笔）；**浸泡式负载**（12 线程×15,000=18 万笔交易 + 9 万+ 次混合 DB 操作，~12s 完成，100% 提交率/金额全局守恒/税额精确）；dynmap XSS 转义回归（script/img/属性逃逸向量）。addons 中 dynmap 已接入 surefire；quests 为 JavaPlugin 启动期一行常量修正，无法脱离服务器实例化，回归锚点为源码行与 commit。核心资金回归：QSEconomyTransactionTest（税额入账/金额守恒/分成防护/onFailed/回滚补偿）、SimpleTradeServiceTest（零单位/溢出防护）、SimpleInventoryTransactionTest（回调契约/LIFO 快照回滚）。测试基建：JUnit 5.11 + Mockito 5.14（静态 mock Bukkit/QuickShop/Util，MockBukkit 引导类接线 ServicesManager 链）。

第八轮（Loop 32-34）验证为安全，**零新缺陷**：quickshop-api 事件体系（63 个事件共享 AbstractQSEvent 的 HandlerList——Paper 的 EventExecutor 带 isInstance 守卫且本项目仅支持 Paper，属作者设计而非缺陷；PhasedEvent 三阶段 clone+phase.cancellable 契约一致）；platform/paper 编解码（serializeAsBytes+Base64，异常由 ShopLoader future 兜底，产出物经 shopNullCheck+unitSize 双重防御）；数据库迁移链 v3→v20（每步 fastBackup+try/catch 幂等重试安全+版本仅在链尾写入+全部 join 同步推进）；menu/browse/keeper/history（物主名为受限字符集、物品名用枚举、价格路径经 PriceLimiter 拦截 Infinity/负数）。**已知接受的设计面**：文本管线（木牌/信息面板/菜单）将物品显示名/商店名作为消息参数参与 MiniMessage 解析——属标准 MC 注入类别（需受害者点击、以受害者自身权限执行），与上游 QuickShop 行为一致，全面转义会破坏物品名着色生态，未改动。上轮两处修复（#19/#20）已补回归测试（TradeMenuQuantityTest×6、SignUpdateWatcherTest×2）。

第七轮（Loop 28-30）验证为安全：SimpleShopCache（删除即全命名空间失效+3min 过期+同引用就地更新，仅保护监听器使用缓存）；VaultProvider/VaultUnlockedProvider（null 守卫/异常捕获/响应检查/服务热重绑）；QSEconomyManager（主线程读写注册表）；ShopDataSaveWatcher（isDone 防重入）；LogWatcher（并发队列+弱一致迭代）；CalendarWatcher（单线程独占 YAML+主线程发事件）；DisplayAutoDespawnWatcher（主线程距离检查）。菜单/木牌两处修复因 TNML 框架耦合无直接单测，锚点为源码+commit（菜单防护与已测的聊天路径 parseTradeAmount 同构）。

第六轮收敛扫描（Loop 25）验证为安全：pl3xmap（占位符仅受限字符集）、bluemap/squaremap（同）、util/matcher（类型+元数据对称严格匹配，无可混换漏洞）、util/updater（异常全捕获）、util/reporter（隐私审查+主线程拒绝+仅 QS 栈帧行）、util/paste（quickshop.paste 权限门控）、towny purgeShops（异步只读+regionThread 删除）、全部 compat 模式扫描（无裸 getArgs/无拼接 SQL/无非主线程 setExtra）。**该轮零新缺陷。**

此前各轮验证为**安全**的部分：SQL 全参数化（无注入面）；价格路径（actionCreate/SubCommand_Price/ShopUtil.setPrice 三处均过 PriceLimiter，默认 min=0 拦负价）；收益命令有 0<percent<100 校验且校验在事件修改之后；interactiveManager 退出/移动清理（无泄漏）；rateLimit/adventureWorkaround 用 ExpiringSet 自过期；聊天交易 TOCTOU 防护（Info.hasChanged JSON 快照比对 + shopIsNotValid 二次校验）；OngoingFeeWatcher 删店前手动余额预检；ContainerShop 全文精读（update CAS 防重入/setShopId 防重设/三阶段事件完整）；AbstractShopManager 三层查找表全并发 Map；silent 命令有全量扫描兜底与 UUID 解析防护；所有权命令均有 OWNERSHIP_TRANSFER 鉴权。

### 已知架构权衡（非缺陷，勿"顺手修复"）
- 库存过户成功但经济提交失败时，无自动逆向库存过户（SimpleShopManager.actionSelling），仅日志告警。这是"先库存后经济"顺序的既定权衡。
- checkTax 失败只回调 onTaxFailed，不回滚主交易（税是事后动作）。
- QSEconomyTransaction 构造器内触发事件，事件监听器若调用 amount()/toTax() 等 setter，已算好的 fromAmount/totalTax（final）不会重算——API 使用者应只用 builder 传税参。
- `to == null` 是有意的"钱消失"语义（建店费、无限商店）。
- **交易后木牌批处理（R10）**：交易成功的告示牌刷新延后至多一个 SignUpdateWatcher 周期（≤0.5s，与漏斗路径一致）；locale 为交易者语言。`shop.immediate-trade-sign-updates: true` 恢复逐笔渲染。
- **库存事务回滚快照不裁剪**：每笔交易双操作各拍全量快照（~95 次克隆）是补偿回滚的保险，预检通过后失败概率低但非零；裁剪触碰物品安全红线，已评估放弃。
- **ContainerShop.matches 的免克隆直通仅对内建 QuickShop 匹配器生效**：第三方 ItemMatcher（无只读契约）仍走防御拷贝+数量归一分支——写自己的匹配器时不要假定会收到原栈。

## 3. 扩展开发指南

### 3.1 新增 /qs 子命令
1. `QS/command/subcommand/` 下新建 `SubCommand_Xxx implements CommandHandler<PlayerCommandSender>`（参考 SubCommand_SilentBuy，模板极稳）。
2. 实现 `onCommand`（权限已由 SimpleCommandManager 统一检查）与 `onTabComplete`。
3. 在 `SimpleCommandManager` 构造器用 `CommandContainer.builder()` 注册。

### 3.2 新增兼容模块（compatibility/）
1. 新建模块目录：pom.xml（依赖 quickshop-api + compat-common + 目标插件 provided）+ plugin.yml（depend 目标插件、softdepend QuickShop-Hikari）。
2. `Main extends CompatibilityModule`：基类已提供 onLoad/onEnable/getShops/recordDeletion/热重载钩子，只需实现 `init()`（读配置+注册第三方监听）。
3. 集成点通常是：注册目标插件的保护检查 + 监听 ShopCreateEvent/ShopPermissionCheckEvent/ShopPurchaseEvent 做拦截。
4. 登记：根 pom.xml `<modules>` + mc-publish.yml 上传列表。
5. 参考 worldguard/Main.java（225 行，最完整范例）。注意 itemsadder 模块因外部依赖无 Maven 仓库被 CI 排除。

### 3.3 数据库 schema 变更
1. `DatabaseUpgrade` 增加版本 N→N+1 方法（内含 fastBackup）。
2. `DataTables` 枚举同步改 DDL 工厂。
3. `SimpleDatabaseHelperV2.java:60` 的 `LATEST_DATABASE_VERSION` 递增。
4. 迁移错误会毁库——必须在 MySQL 与 H2(MODE=MYSQL) 双后端验证。

### 3.4 新增控制面板按钮
实现 `ControlComponent`：`generate(componentHolder, player)` 产出按钮（内部拼 `/qs silentbuy {runtimeUUID}` 之类 silent 命令字符串）+ `getClickType()`。参考 ShopModeComponent。

### 3.5 新增语言键
messages.yml 嵌套 YAML → 代码用 `QuickShop.getInstance().text().of(sender, key, args...).send()` 消费；缺键回退 en_us。44+ 语言经 Crowdin 分发。

## 4. 开发纪律（违反会引入并发/资金 bug）

1. **动钱必走 QSEconomyTransaction.builder()...safeCommit()**，绝不直接调 EconomyProvider。
2. **动库存必走 SimpleInventoryTransaction.failSafeCommit()**。
3. **方块/告示牌操作必须 `QuickShop.folia().getScheduler()` 调度**（Folia 区域线程）；主线程断言用 `Util.ensureThread`。
4. **SQL 只走 sqlManager 的 executeFuture/executeAsync**（虚拟线程池），不在主线程碰数据库。
5. **属性变更走三阶段事件**（PRE→MAIN 可取消→POST），别绕过事件直改。
6. 商店写库靠 `setDirty + update()`，5 分钟 watcher 兜底，不要自己写即时 UPDATE。
7. 新增监听器继承 `AbstractQSListener`（自动热重载注册/注销）。
8. **动缓存必想失效**：SimpleDatabaseHelperV2 三缓存（TTL 10min + 显式失效）、SimpleTextManager 三缓存（reset/register 失效）、QUser 驻留（TTL 5min）、查找表二级索引（注册/注销/setShopId/所有权转移四处维护）——新增删除/导入/换绑数据的代码路径时必须同步接失效钩子。
9. **性能改动必须过基准**：`benchmark/` 前后各 3 fork 对比（注意本机存在 ±15~45% 会话级漂移，重要结论用同批次交替测量归因，方法学见 note/report/perf）。

## 5. 构建

```bash
mvn package -T 1.5C -P github        # 产出 quickshop-bukkit/target/QuickShop-Hikari-*.jar
# 首次运行时插件会经 libby 联网下载运行时依赖（MySQL 驱动、Adventure 等）
```
