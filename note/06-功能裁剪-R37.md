# 功能裁剪（R37，2026-09-01）

> 本轮按用户指定清单移除八类功能，基线 `31cfed2a7`（R36 收尾），收尾 `f5153b8e4`。
> 原则：**只删清单内的功能**；同链路的死配置/死键随功能一并清理；DB schema 与审计日志链不动；其余功能不受影响。

## 移除清单（8 项）

| # | 功能 | 提交 | 处置要点 |
|---|---|---|---|
| 1 | VaultUnlocked 经济后端 | 9122354db | 删 VaultUnlockedProvider；EconomyLoader 删 economy-type 分支与 Vault 2.x 探测，固定 Vault 1.x；`net.milkbowl.vault.economy` 原由 VaultUnlockedAPI jar 传递提供，显式补 `com.github.MilkBowl:VaultAPI:1.7`（provided） |
| 2 | 多币种商店（/qs currency、other.currency、SET_CURRENCY） | 13c9b6588 | 全链路签名净化：EconomyProvider 删 multiCurrency/supportsCurrency 且 format/balance/deposit/withdraw/transfer 去 currency 参数（Vault 1.x 下本是死参数）；ShopMeta/ContainerShop/ShopCurrencyEvent/ShopPrice.format/ShopManager.format/PriceLimiter（币种匹配维度 + price-restriction.yml currency 节）/EconomyFormatter（alternate-currency-symbol-list 币种符号映射）/QuickShop 全局 currency 键/SubCommand_Currency/权限两节点/SuggestPrice 币种过滤；**DB 层保留**：qs_data.currency 列（schema v20 稳定，读写路径已断，值恒 null）、qs_log 审计链（DataRecord/SimpleDataRecord/EconomyTransactionLog/insertTransactionRecord/MetricQuery）原样保留；SET_CURRENCY 枚举值删除安全——商店权限存**组名字符串**非枚举名 |
| 3 | 联网更新检测（含 Rollbar/bStats/FastStats 遥测、mojangapi 死代码） | 286c18563 | 删 util/updater（UpdateManager+Modrinth/Nexus）、UpdateWatcher、api/updater 契约、RollbarErrorReporter（auto-report-errors）、util/metric 遥测管线（bStats/FastStats 上报与 StatCollector 聚合）、util/mojangapi（无接线死代码）、downloads.json（Nexus 下载源清单）；本地 qs_log 交易指标的 metric/ 包（MetricBatcher/MetricListener）**不受影响**；quickshop.alerts 权限保留（商店保护警报仍在用）；PrivacyController 隐私闸保留给 /qs paste，MetricDataType 枚举移入 privacy 包 |
| 4 | Crowdin OTA（仅保留中文，翻译本地化） | 07560be6d | 中文翻译固化到 jar 内 `lang/zh_cn/messages.yml`（loadBundled 路径；内置 en_us 兜底与 fillMissing 补缺机制不变）；删根 crowdin/ 目录（44 语言源）、crowdin.yml、sync-crowdinota.yml 工作流、mc-publish 的 S3 上传与 CloudFlare 清缓存步、SimpleTextManager 的 CrowdinOTA 实例与拉取段、crowdinota 依赖与 ../crowdin 资源目录；jar 内语言文件验证仅剩 en/zh_cn 两个 messages.yml；非中文客户端语言回落 en_us |
| 5 | BungeeCord 跨服消息（+ bungeecord/bungeecord-geyser/velocity 三模块） | c82328645 / 67075b478 | 删 BungeeListener（跨服聊天转发）、QuickShop 的 BungeeCord 频道注册、MsgUtil 的离线消息频道转发与 sendBungeeMessage、bungee-cross-server-msg 配置与语言键；InteractiveManager（聊天价格 pending）退化为纯本地并发 Map；**保留** Util.checkIfBungee 本地代理检测（仅 /qs paste 诊断展示，不联网不收发）与 proxy.force-bungeecord 配置 |
| 6 | addon：dynmap/bluemap/squaremap/pl3xmap/discordsrv/reremake-migrator | 67075b478 | 目录删除 + 根 pom modules/版本属性 + mc-publish 依赖列表；保留 addon：displaycontrol/discount/limited/list/plan/quests/shopitemonly（betonquest 维持原有停用状态） |
| 7 | compat 14 个保护模块：worldguard/towny/lands/plotsquared/griefprevention/bentobox/husktowns/superiorskyblock/iridiumskyblock/fabledskyblock/simpleclaimsystem/ultimateclaims/chestprotect/advancedregionmarket | 67075b478 | 目录删除；**保留** residence/dominion/matcherplus/itemsadder/slimefun/ecoenchants/elitemobs/reforges/clearlag/voidchest/openinv（angelchest 维持原有停用状态） |
| 8 | 保留模块对已删 API 的同步 | f5153b8e4 | addon/discount（4 处）、addon/plan（3 处）、addon/list（1 处）的 shop.getCurrency() 随新签名删除 |

终末残留清理（f8e00e8fa）：messages.yml（en/zh_cn）删除已移除模块的 addon.* 语言键（towny/worldguard/lands/plotsquared/griefprevention/bluemap/dynmap/discord/reremake-migrator/chestprotect/fabledskyblock/superiorskyblock），保留模块键不动；config.yml 删 alternate-currency-symbol-list。

## 联网行为边界（移除后）

| 联网点 | 处置 | 理由 |
|---|---|---|
| 更新检测 / Crowdin OTA / Rollbar / bStats·FastStats | **移除** | 自动联网上报类（用户点名 + 「等联网功能」口径） |
| libby 运行时装库（首次联网，libraries.maven） | 保留 | 启动必需（MySQL 驱动等），离线缓存后不再联网 |
| FastPlayerFinder（Mojang API UUID↔用户名） | 保留 | 功能性按需查询（浏览页/历史的物主名显示），非自动上报；移除会降级显示 |
| /qs paste（LuckoPastebin 上传） | 保留 | 管理员主动诊断操作，经 PrivacyController 隐私闸 |
| Unirest 依赖 | 保留 | 上述两项仍在用 |

## 保留但不受影响的易混淆项

- `EnvironmentChecker.rereMakeTest` 与 ContainerShop 的 Reremake 旧牌子 namespaced key 读取：属**环境冲突检测与旧数据读取兼容**，非迁移器功能，保留。
- `qs_data.currency` 列：DDL 保留（迁移链 v3→v20 稳定），DAO 已无读写路径。
- 交易日志链（qs_log_purchase/transaction 的 currency 字段）：审计数据面，无调用方的 insertTransactionRecord API 原样保留。

## 验证

- quickshop-bukkit **177 用例全绿**（`-Dtest='!TradeLoadSmokeTest'`；dynmap 7 用例已随模块删除）。
- **TradeLoadSmokeTest 浸泡用例（18 万笔 + 9 万 DB 操作）在当前机器超时**：1g 堆下为 12×OOM（surefire 堆受限），3g 堆下为纯 300s 超时（零 OOM、零事务失败）；经 R36 基线 `31cfed2a7` 完整检出同条件复测同样超时（R36 时代表现 ~12s），确认为本机吞吐劣化的环境问题，非本轮回归；其资金守恒/税额断言逻辑由 QSEconomyTransactionTest 等核心回归覆盖。
- 全仓库打包通过（`-pl '!compatibility/matcherplus'`）：主 jar + 7 addon + 10 compat 全部产出；**matcherplus 为既有环境问题**（ExcellentCrates 6.5.0 构件在配置仓库中不存在，本轮未触碰该模块 pom），CI 环境可正常解析。
- benchmark 模块编译通过（EconomyBench/TradeBench/DbBench/Shops 同步新签名；TradeBench 的 formatter mock 改挂 format(double, World)）。
- 主 jar 内容验证：语言文件仅 en_us 兜底 + zh_cn 两个 messages.yml。

## 新增/删除的权限节点

删除：`quickshop.currency`、`quickshop.other.currency`（moderator 包内两条声明同步删除）。无新增。
